# Core Django Framework
django==5.1.7
djangorestframework

# REST APIs, Middleware
django-cors-headers

# Speech Recognition & TTS
SpeechRecognition==3.10.0
pyttsx3==2.90
gtts

fuzzywuzzy==0.18.0
chatterbot ==1.2.4
# Text & Language Processing
spacy==3.8.4
nltk==3.9.1
textblob==0.19.0
vaderSentiment==3.3.2

# Sentiment & Spell Check
python-dateutil==2.9.0.post0

# For MongoDB if needed
pymongo

# Google AI (Optional, based on presence in original requirements)
google-generativeai

# Utilities
comtypes==1.1.14; platform_system=="Windows"
pywin32==306; platform_system=="Windows"

# Optional or Development use only
gunicorn==23.0.0  
# Model downloads (required only during development or setup)
#python -m nltk.downloader wordnet
#python -m spacy download en_core_web_sm
